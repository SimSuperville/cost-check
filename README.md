# cost-check
 A Claude Code slash command that shows token usage for your last message and full session, with progress bars benchmarked against Pro ($20), Max 5x ($100), and Max 20x ($200) plan  windows. Reads session JSONL files locally — no API calls, no auth. Install as a plugin and run /cost-check to see exactly how much of your 5-hour window you've burned.
