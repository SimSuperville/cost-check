---
description: Show token usage for the last message and full session, benchmarked against Pro, Max 5x, and Max 20x plan windows
---

1. Run `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/cost-check.py` using the Bash tool.
2. Then send a message to the user containing the full stdout of that command wrapped in a triple-backtick code block. No intro, no commentary, no extra text before or after the code block — just the code block.
